stages:
  - build_and_publish

variables:
  IMAGE_TAG: "gitlab.upt.ro/bianca.mustea/proiect-sincretic-2024"

build_and_publish:
  stage: build_and_publish
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "$CI_REGISTRY_PASSWORD" | docker login gitlab.upt.ro -u bianca.mustea --password-stdin
    - docker build . -t $IMAGE_TAG
    - docker push $IMAGE_TAG
  only:
    - main
